// Use it imported from search.ts. This is here because we use it in types.d.ts as well.
export const NONE: unique symbol = Symbol("none")
export const FILTERS_PROGRESS: unique symbol = Symbol("filters progress")
