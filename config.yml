globals:
  sites:
    - domain: localhost
      index: peerdb
      schema: peerdb
      features:
        searchResultsTable: true
        createButtons: true
        editButtons: true
        downloadButtons: true
